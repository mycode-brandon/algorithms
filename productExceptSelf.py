# Without being able to divide, makes this interesting. I felt like there needed to be some utility arrays to
# get this complete, but couldn't quite figure our the trick. After some research, found the prefix/postfix method and
# that can even be implemented without creating any arrays other than the result.

def productExceptSelf(nums: list[int]) -> list[int]:

